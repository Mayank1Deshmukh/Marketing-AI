{
  "buildCommand": "pnpm --filter @workspace/marketing-agent run build && pnpm --filter @workspace/api-server run build",
  "outputDirectory": "artifacts/marketing-agent/dist/public",
  "installCommand": "pnpm install",
  "framework": null,
  "functions": {
    "api/index.mjs": {
      "memory": 1024,
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index" },
    { "source": "/:path*", "destination": "/index.html" }
  ]
}
